extends layout

block header
  h1 Bitcoin Data

block content
  .col-md-6
    h3.text-center Current Value: US$ #{last.toFixed(3)}

  .col-md-6
    h3.text-center Average Value: US$ #{average.toFixed(3)}
