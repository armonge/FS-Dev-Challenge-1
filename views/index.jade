extends layout

block header
  h1= title

block content
  .col-md-6
    h3.text-center Current Value: US$ #{bcData.last.toFixed(3)}

  .col-md-6
    h3.text-center Average Value: US$ #{bcData.average.toFixed(3)}
